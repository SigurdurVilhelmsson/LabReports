# DEPRECATED - This file is no longer used
# Application is deployed on Linode with Node.js backend
# See DEPLOYMENT.md for current deployment instructions

[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  node_bundler = "esbuild"
